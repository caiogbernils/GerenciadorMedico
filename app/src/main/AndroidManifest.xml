<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.bernilscaio.gerenciadormedico">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdicionarConsultasActivity"></activity>
        <activity android:name=".ListarConsultasActivity" />
        <activity android:name=".EditarConsultasActivity" />
        <activity android:name=".AdicionarPacienteActivity" />
        <activity android:name=".ListarPacienteActivity" />
        <activity android:name=".EditarPacienteActivity" />
        <activity android:name=".AdicionarMedicoActivity" />
        <activity android:name=".ListarMedicoActivity" />
        <activity android:name=".EditarMedicoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>